{
  "name": "my-buddy",
  "type": "module",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev:local": "astro dev",
    "build": "bun run build:wiki-index && astro build && pagefind --site dist",
    "build:wiki-index": "bun src/plugins/wiki-links/build-index.ts",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync": "bash scripts/sync.sh"
  },
  "dependencies": {
    "astro": "^5.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@tailwindcss/typography": "^0.5.16",
    "@tailwindcss/vite": "^4.0.0",
    "pagefind": "^1.3.0",
    "tailwindcss": "^4.0.0",
    "unist-util-visit": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
